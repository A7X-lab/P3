#include <iostream>

int main() {
    const double Rate = 0;
    const double initialAmount = 0;
    const double TRGET = 2 * initialAmount;

    double Amount = initialAmount;
    int year = 0;

    while (balance < Target)
        (
                year++
                double interest = balance + Rate/ 100;
                balance = balance + interest;
        )

        cout << "The investment is"
                )

    return 0;
}
